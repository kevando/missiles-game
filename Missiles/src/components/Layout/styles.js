import { StyleSheet, Dimensions } from 'react-native';
import { colors } from '../../config/styles';


export default StyleSheet.create({

  headerWrapper: {
    backgroundColor: '#eee',//colors.inputBackground,
    padding: 10,
    borderColor: '#aaa',
    borderBottomWidth: 1
  },
});
